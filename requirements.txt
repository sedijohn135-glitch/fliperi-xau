# The Open API stack is gone: no ctrader-open-api, no Twisted, no protobuf, and
# with them the dependency conflict that stopped this project building at all
# (ctrader-open-api 0.9.2 pins Twisted 24.3 and protobuf 3.20, while the old
# requirements asked for Twisted 24.7 and protobuf >=4.25).
mcp>=1.9.0,<3.0.0
httpx>=0.27.0
fastapi>=0.115.0
uvicorn>=0.34.0
python-multipart>=0.0.20
tzdata>=2024.1
